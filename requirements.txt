# Python dependencies for the sparse-paleoclimate-field-reconstruction project.

# TODO: Further downstream, pin versions so experiments are reproducable against same library versions.
# Installed into the venv at ./paleo (gitignored).

# ---- Core scientific Python stack -------------------------------------------
numpy
pandas
matplotlib
scikit-image          # SSIM for field reconstructions (paleoreco.eval.da)

# ---- Machine learning -------------------------------------------------------
torch
scikit-learn          # PCA, TruncatedSVD, KMeans in eval/ and assim/experiments

# ---- Domain-specific --------------------------------------------------------
xarray                # labelled grids for the CCSM4 -> LOVECLIM regrid
xarray-regrid         # conservative rectilinear regridding, pure-Python (no ESMF)

# ---- Data acquisition -------------------------------------------------------
# Used only by paleoreco/data/download_ccsm4.py to pull the CCSM4 truth run from
# ERDA; not imported by the rest of the package.
fsspec[http]          # HTTP range reads of the remote run (pulls aiohttp)
h5netcdf              # NetCDF engine for xarray.open_dataset (pulls h5py)

# ---- Notebook + dev experience ---------------------------------------------
ipykernel
tqdm
pytest                # unit + integration test suite (tests/)
